export enum TasksStatus {
  PENDING = 'pending',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
}

export enum TasksPriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
}

export enum SortBy {
  STATUS = 'status',
  PRIORITY = 'priority',
  DUE_DATE = 'dueDate',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}
